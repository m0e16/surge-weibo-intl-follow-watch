{
  "name": "surge-weibo-intl-follow-watch",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check scripts/weibo_intl_follow_watch.js && node --check scripts/boxjs_sync.js"
  }
}
